<nav style="background: #101734">
    <div class="container mx-auto rem-0.375 py-2 px-2 flex justify-between items-center">
        <a class="font-bold text-2xl lg:text-4xl text-white" href="{{ path('app_terminologio_index') }}">
            Terminologio
        </a>
        <div class="block lg:hidden">
            <button class="flex items-center rem-0.125 py-2 border rounded text-gray-500 border-gray-600 hover:text-gray-800 hover:border-teal-500 appearance-none focus:outline-none">
                <svg class="fill-current h-3 w-3" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
                    <title>Menu</title>
                    <path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z" />
                </svg>
            </button>
        </div>
        <div class="hidden lg:block">
            <ul class="inline-flex space-x-10">
                <li><a class="rem-0.25 font-bold text-white" href="/">Home</a></li>
                {% if not is_granted('ROLE_USER') %}
                    <li><a class="rem-0.25 hover:text-gray-700 text-white" href='{{ path('app_login') }}'>Sign in</a></li>
                    <li><a class="rem-0.25 hover:text-gray-700 text-white" href="{{ path('app_register') }}">Sign up</a></li>
                {% else %}
                    <li><a class="rem-0.25 hover:text-gray-700 text-white" href="{{ path('app_logout') }}">Logout</a></li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>