version: '3'

services:
  server-web:
    command: bash -c "cd /app
      && symfony server:ca:install
      && symfony server:start"
    volumes:
      - .:/app
      - /app/vendor
      - /app/var
    ports:
      - "8000:8000"

###> doctrine/doctrine-bundle ###
###< doctrine/doctrine-bundle ###